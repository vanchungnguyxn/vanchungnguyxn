{
  "name": "vanchungnguyxn-profile",
  "private": true,
  "type": "module",
  "scripts": {
    "render": "node scripts/render-metrics.js",
    "export": "node scripts/export-png.js",
    "build": "npm run render && npm run export"
  },
  "engines": {
    "node": ">=20"
  }
}
